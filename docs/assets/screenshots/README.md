# Screenshots

Captured from a real Streamlit session with live data (e2e_demo dataset, MLflow runs, registered models).

## Captured

- `dashboard-overview.png`: workspace overview with 8 recent jobs and 6 tracked datasets
- `dataset-intake.png`: loaded e2e_demo dataset (200 rows, 5 columns) with preview table
- `validation-summary.png`: 5/5 checks passed with target column selected
- `prediction-center.png`: prediction center with discovered local model and recent prediction jobs
- `history-view.png`: 6 MLflow benchmark runs with sort/filter controls
- `registry-view.png`: 3 registered models with alias and version detail

## Not Yet Captured

- `profiling-report.png`: profiling summary cards and artifact output
- `benchmark-leaderboard.png`: leaderboard and benchmark summary metrics
- `experiment-lab.png`: compare leaderboard plus tune/evaluate/save actions (requires PyCaret on Python 3.11/3.12)
